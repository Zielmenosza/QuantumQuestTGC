#ifndef PLAYER_ID_H
#define PLAYER_ID_H

// Define your PlayerID type here. It could be an enum, a class, or a simple typedef.
enum class PlayerID {
    PLAYER_ONE,
    PLAYER_TWO,
    // ... add moreplayer IDs if needed
};

#endif // PLAYER_ID_H